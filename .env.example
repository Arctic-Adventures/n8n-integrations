# Domain and URL Configuration
# For IP-only access use: DOMAIN=YOUR_SERVER_IP
# For domain access use: DOMAIN=yourdomain.com
DOMAIN=157.180.115.170
WEBHOOK_URL=http://157.180.115.170:5678

# n8n Configuration
N8N_PORT=5678
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change_this_password
N8N_SECURE_COOKIE=false  # Set to false for HTTP (non-SSL) access

# Database Configuration
POSTGRES_DB=n8n
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=change_this_secure_password

# Email Configuration (Optional - leave empty if not needed)
N8N_SMTP_HOST=
N8N_SMTP_PORT=
N8N_SMTP_USER=
N8N_SMTP_PASS=
N8N_SMTP_SENDER=

# SSL Certificate Configuration
# Email for Let's Encrypt notifications (required even if not using domain)
ACME_EMAIL=admin@example.com

# Timezone
GENERIC_TIMEZONE=Atlantic/Reykjavik

# Traefik Authentication 
# Generate with: htpasswd -nb admin yourpassword
# Then double all $ symbols when adding to this file
TRAEFIK_AUTH=admin:$$apr1$$xxxxx$$xxxxxxxxxxxxxxxxxx